# last_run_report.json is regenerated by the large accuracy test on
# every invocation. Keep it out of git so the working tree stays clean
# across runs and CI archives the artefact instead. baseline_report.json
# (the committed snapshot the regression test diffs against) IS tracked.
last_run_report.json
